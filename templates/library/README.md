# Library Template

Use this template when reusable importable API is primary, with optional pipeline orchestration.

## Included patterns

- Public API in `__init__.py`
- Pure helper module (`api.py`)
- Optional thin `main.py` for CLI execution
- Shared pipeline scaffold and tests
